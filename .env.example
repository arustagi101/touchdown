# OpenAI API Key (Required)
OPENAI_API_KEY=your-openai-api-key-here

# Secret key for JWT signing (can be any secure string for local development)
SECRET_KEY=your-secret-key-for-local-development

# Database Configuration (defaults work for Docker setup)
DATABASE_URL=postgresql://touchdown:touchdown_pass@localhost:5432/touchdown
REDIS_URL=redis://localhost:6379

# CORS Origins (adjust if frontend runs on different port)
CORS_ORIGINS=http://localhost:3000

# Optional: Video processing limits
MAX_VIDEO_SIZE_MB=500
MAX_PROCESSING_TIME_MINUTES=30

# Optional: AWS S3 (not needed for local development)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=touchdown-videos
# AWS_REGION=us-east-1
